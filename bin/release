#!/usr/bin/env bash
echo "Profile file setting:"
source compile.sh
echo "Deployment Path: $DEPLOYMENT_PATH"
cat <<EOF > Procfile
    web: $DEPLOYMENT_PATH/approot/web --server.urls http://+:$PORT
EOF
