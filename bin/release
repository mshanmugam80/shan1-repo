#!/usr/bin/env bash
source compile.sh
topic "Deployment Path: $DEPLOYMENT_PATH"

topic "Profile file setting:"
cat <<EOF > Procfile
    web: $DEPLOYMENT_PATH/approot/web --server.urls http://+:$PORT
EOF
